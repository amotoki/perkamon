--- C/man4/random.4	2013-03-16 00:20:08.133670690 +0100
+++ C/man4/random.4	2013-03-16 00:21:05.705956068 +0100
@@ -252,17 +252,15 @@
 which only adds some
 data but does not increment the entropy count.
 The following structure is used:
-.IP
-.IN +4n
-.NF
-struct rand_pool_info {
-    int    entropy_count;
-    int    buf_size;
-    __u32  buf[0];
-};
-.FI
-.IN
-.IP
+
+.nf
+    struct rand_pool_info {
+        int    entropy_count;
+        int    buf_size;
+        __u32  buf[0];
+    };
+.fi
+
 Here
 .I entropy_count
 is the value added to (or subtracted from) from the entropy count, and
